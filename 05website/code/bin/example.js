// Generated by Haxe 4.1.4
(function ($global) { "use strict";
var Express = require("express");
class Main02 {
	constructor() {
		let app = Express();
		let port = 3000;
		console.log("src/Main02.hx:17:","Express website (02):  open browser at http://localhost:" + port);
		console.log("src/Main02.hx:18:","Stop node.js : CTRL + c");
		app.set("port",3000);
		let app1 = app;
		let tmp = ServeFavicon(__dirname + "/public/favicon.ico");
		app1.use(tmp);
		app.set("views",__dirname + "/public/views");
		app.set("view engine","pug");
		app.use(Morgan("dev"));
		let app2 = app;
		let tmp1 = Express.static;
		let tmp2 = js_node_Path.join(__dirname,"public");
		app2.use(tmp1(tmp2));
		app.get("/",function(req,res,next) {
			return res.send("Hello World! :: Express website (02)");
		});
		app.get("/remote",function(req,res,next) {
			res.sendFile(__dirname + "/public/remote_intermediate.html");
			return null;
		});
		app.get("/pug",function(req,res,next) {
			res.render("index",{ title : "Home", h1 : "Title"});
			return null;
		});
		app.get("/api/users",function(req,res,next) {
			let username = req.param("username");
			res.send("username: " + username);
			return null;
		});
		app.post("/api/users",function(req,res,next) {
			let _req = req;
			let _username = _req.body.username;
			res.send("_username: " + _username);
			return null;
		});
		app.use(function(req,res,next) {
			return res.status(404).send("404");
		});
		app.listen(app.get("port"),function() {
			console.log("src/Main02.hx:84:","Express server listening on port " + Std.string(app.get("port")));
		});
	}
	static main() {
		let main = new Main02();
	}
}
Main02.__name__ = true;
Math.__name__ = true;
var Morgan = require("morgan");
var ServeFavicon = require("serve-favicon");
class Std {
	static string(s) {
		return js_Boot.__string_rec(s,"");
	}
}
Std.__name__ = true;
class haxe_iterators_ArrayIterator {
	constructor(array) {
		this.current = 0;
		this.array = array;
	}
	hasNext() {
		return this.current < this.array.length;
	}
	next() {
		return this.array[this.current++];
	}
}
haxe_iterators_ArrayIterator.__name__ = true;
class js_Boot {
	static __string_rec(o,s) {
		if(o == null) {
			return "null";
		}
		if(s.length >= 5) {
			return "<...>";
		}
		let t = typeof(o);
		if(t == "function" && (o.__name__ || o.__ename__)) {
			t = "object";
		}
		switch(t) {
		case "function":
			return "<function>";
		case "object":
			if(((o) instanceof Array)) {
				let str = "[";
				s += "\t";
				let _g = 0;
				let _g1 = o.length;
				while(_g < _g1) {
					let i = _g++;
					str += (i > 0 ? "," : "") + js_Boot.__string_rec(o[i],s);
				}
				str += "]";
				return str;
			}
			let tostr;
			try {
				tostr = o.toString;
			} catch( _g ) {
				return "???";
			}
			if(tostr != null && tostr != Object.toString && typeof(tostr) == "function") {
				let s2 = o.toString();
				if(s2 != "[object Object]") {
					return s2;
				}
			}
			let str = "{\n";
			s += "\t";
			let hasp = o.hasOwnProperty != null;
			let k = null;
			for( k in o ) {
			if(hasp && !o.hasOwnProperty(k)) {
				continue;
			}
			if(k == "prototype" || k == "__class__" || k == "__super__" || k == "__interfaces__" || k == "__properties__") {
				continue;
			}
			if(str.length != 2) {
				str += ", \n";
			}
			str += s + k + " : " + js_Boot.__string_rec(o[k],s);
			}
			s = s.substring(1);
			str += "\n" + s + "}";
			return str;
		case "string":
			return o;
		default:
			return String(o);
		}
	}
}
js_Boot.__name__ = true;
var js_node_Path = require("path");
String.__name__ = true;
Array.__name__ = true;
js_Boot.__toStr = ({ }).toString;
Main02.main();
})({});
